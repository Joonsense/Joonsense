# YieldScope

모든 암호화폐 수익률을 한 곳에서 확인하세요. 이 레포는 Supabase, Next.js, n8n을 이용한 대시보드 예시입니다.

## 로컬 실행
```bash
#> pnpm install --frozen-lockfile
#> pnpm --filter ./next-app dev
```

## Vercel 배포
1. `.env` 파일에 Supabase와 API 키를 입력하세요.
2. Vercel 프로젝트에 연결 후 배포합니다.

English below.

---

Check crypto yields in one dashboard. Built with Supabase, Next.js and n8n.

### Local Setup
```bash
#> pnpm install --frozen-lockfile
#> pnpm --filter ./next-app dev
```

### Deploy to Vercel
1. Fill in Supabase and API keys in `.env`.
2. Connect the repo to Vercel and deploy.

**1 USD / month on free tiers until 10 k MAU**
